# KeyScan Android / iOS 界面与交互对齐说明

文档用途：记录 Android 端微调内容，供 iOS 端直接据此实现对应设计与交互。

维护规则：

- 每次 Android 界面或交互微调完成后，在本文档追加记录。
- iOS 开发者应以“iOS 实现要求”和“验收标准”为主要实施依据。
- 除非条目明确说明，否则不改变数据结构、加密方式、同步格式或业务数据。

---

## 2026-07-29：第一轮界面微调

Android 验证状态：已完成正式 Release 构建，构建成功。

本轮正式 APK：`Artifacts/KeyScan-v1.0.0-keyscanRelease-latest.apk`

### 1. 首页 KeyScan 品牌标识缩小并上移

#### 调整目标

降低首页顶部 KeyScan 品牌标识的视觉占比，增加顶部区域的留白和平衡感，并让标识位置略微靠近页面顶部。

#### Android 调整

- 页面：主导航首页顶部区域。
- 真实入口：应用启动后显示的三页式主导航界面。
- 原显示区域：宽 `136dp`、高 `58dp`。
- 新显示区域：宽 `124dp`、高 `54dp`。
- 相对原位置向上移动约 `2dp`。
- 缩放约为原宽度的 91%，属于轻量缩小，不改变品牌标识本身的绘制比例、渐变色、盾牌图案或副标题。
- 右侧的云同步、添加和更多按钮尺寸与位置不变。
- 顶部区域整体高度仍为 `88dp`，避免影响下面的页面内容和手势区域。

#### iOS 实现要求

- 对应调整 iOS 首页导航区左侧的 KeyScan 品牌组合标识。
- 相对当前 iOS 设计缩小约 8%～10%，保持等比例缩放，不拉伸图形或文字。
- 相对当前垂直位置向上移动约 2pt。
- 不改变顶部导航区总高度。
- 不移动右侧功能按钮；需保证品牌标识和右侧按钮之间仍有安全间距。
- 保持 KeyScan 渐变文字、盾牌图案及品牌副标题的视觉关系不变。
- 若 iOS 使用图片资源，应设置为等比例适配（Aspect Fit）；若使用矢量或自绘视图，应以整体组合图的边界进行缩放。

#### 验收标准

1. 品牌标识比调整前略小，但仍清晰且具有首页品牌识别度。
2. 标识整体比调整前略靠上，不与状态栏或安全区域发生重叠。
3. 云同步、添加、更多按钮的位置和点击区域不受影响。
4. 小屏设备上品牌标识与右侧按钮不重叠。
5. 深色和浅色模式下尺寸及位置一致。

#### Android 对应文件

- `app/src/main/res/layout/fragment_primary_navigation.xml`
- `app/src/main/java/com/secureqr/scanner/ui/home/KeyScanBrandLockupView.java`（绘制内容未修改，仅供 iOS 理解品牌组合结构）

---

### 2. 密码账本文件夹进入页面时默认折叠

#### 调整目标

进入密码账本时先展示文件夹/分组概览，避免默认展开后大量账号记录直接占满页面，让用户自行选择需要查看的文件夹。

#### Android 调整

- 页面：密码账本列表。
- 适用范围：全部文件夹/分组，包括：
  - 默认分组；
  - 用户创建的自定义分组；
  - 系统“安全分享”分组。
- 每次新建密码账本列表适配器、首次进入该页面时，所有分组均为折叠状态。
- 折叠时只显示分组标题行、记录数量、下拉箭头及允许显示的分组菜单。
- 折叠状态下不显示该分组内部的密码记录。
- 点击分组标题行后展开，再次点击则折叠。
- 展开时箭头向上，折叠时箭头向下。
- 页面实例仍然存活期间，用户手动展开或折叠的状态继续保留；重新创建页面后再次恢复为全部折叠。
- “安全分享”分组继续隐藏分组操作菜单，其保护规则不变。

#### 数据与业务影响

- 仅改变列表初始展示状态。
- 不修改密码记录、分组数据或数据库结构。
- 不改变分组排序、记录数量、创建、重命名、删除、分享和同步逻辑。
- 不改变 WebDAV 备份、恢复或跨平台数据格式。
- iOS 不需要为此增加新的持久化字段。

#### iOS 实现要求

- 密码账本页面首次加载时，将所有 section/folder 的 `isExpanded` 初始值设为 `false`。
- 默认分组和安全分享分组不得特殊设为展开。
- 分组标题始终可见，子记录仅在该分组展开时插入或显示。
- 点击整个分组标题行执行展开/折叠，不能仅允许点击箭头。
- 展开/折叠应刷新对应分组，避免整个列表明显闪烁或滚动位置跳动。
- 箭头方向必须与状态一致：折叠向下，展开向上。
- 本轮不要求跨启动保存展开状态；重新进入或重建页面时恢复全部折叠。
- VoiceOver 应朗读分组名称、记录数量以及“已折叠/已展开”状态，并提供展开或折叠操作提示。

#### 验收标准

1. 密码账本首次打开时，只显示分组行，不显示任何分组内部记录。
2. 默认分组、安全分享分组和自定义分组全部默认折叠。
3. 点击任意分组行可正常展开，并显示正确数量的记录。
4. 再次点击同一分组行可折叠。
5. 一个分组的展开状态不应强制改变其他分组；允许多个分组同时展开。
6. 新增、编辑或删除记录后，分组数量和展开后的内容正确更新。
7. 该调整不改变数据库内容、同步内容和加密数据。

#### Android 对应文件

- `app/src/main/java/com/secureqr/scanner/ui/password/PasswordEntryAdapter.java`
- `app/src/main/res/layout/item_password_group.xml`

---

## 2026-07-29：密码账本分组卡片视觉重构

### 参考设计的语义说明

参考图中的“测试”不是系统预设名称，也不是要求创建一个名为“测试”的文件夹。它仅代表用户创建的任意自定义文件夹示例。

iOS 实现时必须读取并显示用户保存的真实文件夹名称，例如“工作”“银行”“社交账号”等。所有用户自定义文件夹共享同一种青色文件夹锁图标和“自定义分组”副标题，但标题不得写死。

### 页面整体结构

密码账本页面继续保留以下结构：

1. 顶部导航栏；
2. 搜索框，提示语为“搜索网站或账号”；
3. 分组卡片列表；
4. 展开分组后，在对应卡片下方显示密码记录。

本轮不修改搜索逻辑、顶部按钮、密码记录卡片或数据操作流程，重点重构分组卡片。

### 分组卡片尺寸与布局

- 单张分组卡片高度：`74dp`；iOS 对应建议 `74pt`。
- 卡片顶部外间距：`4dp`。
- 卡片底部外间距：`6dp`。
- 卡片左右内边距：左 `8dp`、右 `6dp`。
- 卡片沿用应用通用卡片背景、圆角、描边及阴影：
  - 浅色模式为白色卡片和浅灰描边；
  - 深色模式为深蓝灰卡片和较亮蓝灰描边。
- 内容按水平方向排列：
  1. 类型图标；
  2. 标题与副标题；
  3. 记录数量；
  4. 展开/折叠箭头；
  5. 更多操作按钮。

### 图标容器规范

- 图标容器：`46 × 46dp`，iOS 对应 `46 × 46pt`。
- 圆角：`10dp`。
- 图标内边距：`9dp`。
- 图标为线性图标，保持清晰、简洁，不使用 Emoji。
- 三类分组必须使用不同图标和颜色，以便用户快速辨识。

#### 默认分组

- 固定标题：中文“默认分组”；英文“Default group”。
- 副标题：中文“所有未分类的密码”；英文“All uncategorized passwords”。
- 图标：蓝色钥匙图标。
- 浅色模式：
  - 容器底色 `#EAF4FF`；
  - 边框 `#A9D5FF`；
  - 图标 `#008DFF`。
- 深色模式：
  - 容器底色 `#102A4A`；
  - 边框 `#155A91`；
  - 图标 `#19A7FF`。
- 默认分组由系统 ID 判断，不能通过显示名称猜测。

#### 用户自定义文件夹

- 标题：用户实际创建的文件夹名称，不得写死为“测试”。
- 副标题：中文“自定义分组”；英文“Custom group”。
- 图标：青色文件夹加小锁图标。
- 浅色模式：
  - 容器底色 `#E8FFFC`；
  - 边框 `#8EE8DE`；
  - 图标 `#00AFA5`。
- 深色模式：
  - 容器底色 `#073B3D`；
  - 边框 `#087D79`；
  - 图标 `#10D9CC`。
- 无论用户创建多少个文件夹，都使用这套自定义文件夹样式，但每张卡片显示各自真实名称及记录数量。

#### 安全分享分组

- 固定标题：中文“安全分享”；英文“Secure sharing”。
- 副标题：中文“分享的安全密码”；英文“Securely shared passwords”。
- 图标：紫色分享节点加小锁图标。
- 浅色模式：
  - 容器底色 `#F2ECFF`；
  - 边框 `#C8B3FF`；
  - 图标 `#8B5CF6`。
- 深色模式：
  - 容器底色 `#251A4F`；
  - 边框 `#51408E`；
  - 图标 `#A978FF`。
- 安全分享分组由系统 ID 判断。
- Android 当前继续隐藏该系统分组的重命名/删除菜单，避免破坏系统分组。iOS 也不应允许用户重命名或删除安全分享分组。

### 文字规范

- 标题：
  - 字号 `16sp`；iOS 建议 `16pt`；
  - 粗体；
  - 单行，过长时尾部省略。
- 副标题：
  - 字号 `12sp`；iOS 建议 `12pt`；
  - 次要文字颜色；
  - 标题下方间距 `3dp`；
  - 单行，过长时尾部省略。
- 数量：
  - 中文格式：`n 条`；
  - 英文根据单复数显示 `1 item` 或 `n items`；
  - 字号 `13sp`；
  - 最小显示宽度 `38dp`；
  - 数量必须来自当前分组的实际记录数量。

### 展开、折叠与点击行为

- 初次进入页面时，全部分组保持折叠，延续上一条调整。
- 折叠时箭头向下；展开时箭头向上。
- 点击卡片主体或箭头均执行展开/折叠。
- 更多按钮必须独立响应，不能同时触发展开/折叠。
- 自定义文件夹和默认分组保留更多操作入口。
- 安全分享分组不提供重命名和删除操作。
- 展开状态仅属于界面状态，不写入密码数据或同步文件。

### iOS 数据映射建议

iOS 列表模型至少需要在界面层区分：

- `default`：系统默认分组；
- `custom`：用户自定义文件夹；
- `secureShare`：系统安全分享分组。

建议通过稳定 ID 或枚举映射类型，不要依赖中文或英文标题文本。自定义文件夹的 `title` 直接读取用户数据，`subtitle` 使用本地化后的“自定义分组”。

### 无障碍要求

VoiceOver/TalkBack 应组合朗读：

`分组名称 + 分组类型说明 + 记录数量 + 已折叠/已展开`

示例：“工作，自定义分组，3 条，已折叠”。图标为装饰元素，不应被单独朗读。

### 验收标准

1. 默认分组显示蓝色钥匙图标和“所有未分类的密码”。
2. 任意用户自定义文件夹显示真实名称、青色文件夹锁图标和“自定义分组”。
3. 不存在固定写死的“测试”文件夹名称。
4. 安全分享显示紫色分享锁图标和“分享的安全密码”。
5. 三类卡片的记录数量准确。
6. 卡片折叠与展开箭头方向正确。
7. 浅色和深色模式均清晰可读，颜色按上述规范切换。
8. 长文件夹名称不会挤压数量、箭头和菜单，而是在末尾省略。
9. 本轮调整不改变密码、分组、加密、同步或备份数据结构。

### Android 对应文件

- `app/src/main/res/layout/item_password_group.xml`
- `app/src/main/java/com/secureqr/scanner/ui/password/PasswordEntryAdapter.java`
- `app/src/main/res/drawable/ic_password_group_default.xml`
- `app/src/main/res/drawable/ic_password_group_custom.xml`
- `app/src/main/res/drawable/ic_password_group_share.xml`
- `app/src/main/res/drawable/bg_password_group_default_icon.xml`
- `app/src/main/res/drawable/bg_password_group_custom_icon.xml`
- `app/src/main/res/drawable/bg_password_group_share_icon.xml`
- `app/src/main/res/values/colors.xml`
- `app/src/main/res/values-night/colors.xml`

---

## 2026-07-29：恢复保障、安全分享入口与保险箱附件导出

### 1. 安全中心增加“恢复保障”产品状态

#### 设计目的

安全中心负责展示风险和恢复能力；“备份与恢复”页面负责执行备份、验证和恢复。安全中心不再仅以“是否填写 WebDAV”判断备份安全，而是综合四项条件：

1. WebDAV 云端配置是否完整；
2. 数据保护密钥是否设置；
3. 是否存在最近一次成功备份记录；
4. 最近备份是否经过恢复验证。

#### 状态定义

- **未设置**：WebDAV、数据保护密钥或成功备份记录任一缺失。
- **待验证**：已有云端配置、数据保护密钥和备份，但从未验证该备份。
- **已保障**：最新备份已经成功验证，且备份时间不超过 30 天。
- **需更新**：验证失败，或验证时间早于最新备份时间，说明最新备份尚未验证。
- **已过期**：备份已验证，但最后备份距当前时间超过 30 天。

#### 页面和交互

- 安全中心原“安全恢复中心”入口调整为“恢复保障”。
- 副标题为“检查备份是否存在，并验证换机后能否恢复”。
- 安全状态总览中的“备份”状态同步使用上述恢复保障状态，不再只显示 WebDAV 已连接/未设置。
- 点击“恢复保障”进入“备份与恢复”页面。
- 备份与恢复页面继续提供：
  - 立即备份；
  - 验证备份；
  - 恢复数据；
  - 打开 WebDAV 配置。
- 页面顶部增加说明：恢复保障综合检查云端配置、数据保护密钥、最近备份和恢复验证。

#### iOS 实现要求

- 使用与 Android 相同的五态状态机。
- 状态判断必须基于稳定的配置和时间字段，不根据页面显示文案反推。
- `lastBackupAt` 表示最近成功备份时间。
- `lastVerifiedAt` 表示最近恢复验证时间。
- 仅当验证成功、`lastVerifiedAt >= lastBackupAt` 且备份未超过 30 天时显示“已保障”。
- 验证动作只读取、解密并检查备份摘要，不得覆盖当前设备数据。
- 真正恢复前必须再次验证身份、展示备份摘要，并明确提示当前设备已有数据时可能发生合并或替换。

### 2. 删除密码账本右上角安全分享入口

#### Android 调整

- 从密码账本右上角“＋”菜单中删除“安全分享”选项。
- 不再通过右上角菜单打开密码选择器进行分享。
- 密码记录长按分享保持不变：
  1. 用户展开文件夹；
  2. 长按具体密码记录；
  3. 进入安全分享流程。
- 新增密码、创建分组、密码生成记录和密码账本导入入口保持不变。
- 接收安全分享数据以及系统“安全分享”分组保持不变。

#### iOS 实现要求

- 删除密码账本右上角新增菜单中的安全分享按钮/菜单项。
- 不保留其他重复的全局“发起安全分享”按钮。
- 单条密码记录的长按菜单必须保留“安全分享”。
- 分享必须绑定用户长按的那一条密码记录，不再弹出全局密码选择器。
- 建议为 VoiceOver 用户提供等价的上下文菜单操作，避免只能依赖物理长按。

#### 验收标准

1. 右上角菜单中看不到“安全分享”。
2. 长按任意密码记录仍能进入安全分享。
3. 普通点击仍进入记录详情/编辑，不触发分享。
4. 安全分享接收、分享次数状态和安全分享系统分组不受影响。

### 3. 安全保险箱导出支持全部附件

#### 导出格式

保险箱通用导出由单一 JSON 文件升级为 ZIP 文件，默认文件名：

`keyscan_vault_export.zip`

ZIP 内部结构：

```text
keyscan_vault_export.zip
├── vault.json
├── manifest.json
└── attachments/
    └── {vaultItemId}/
        └── {attachmentId}_{原始文件名}
```

#### 文件说明

- `vault.json`
  - 保存保险箱资料的标题、类型、分类、字段、备注、创建时间和更新时间；
  - 数据结构沿用原通用 JSON 导出格式。
- `manifest.json`
  - 保存每个附件与保险箱资料的关联关系；
  - 每个附件条目包含：
    - `id`：附件稳定 ID；
    - `itemId`：所属保险箱资料 ID；
    - `filename`：原始文件名；
    - `mimeType`：MIME 类型；
    - `size`：原始文件大小；
    - `hash`：原始附件 SHA-256；
    - `path`：附件在 ZIP 中的相对路径。
- `attachments/`
  - 保存全部附件原始内容；
  - 以保险箱资料 ID 分目录；
  - 文件名前增加附件 ID，避免同名文件互相覆盖。

#### Android 安全实现

- 附件在应用私有目录中仍保持 Android Keystore 加密存储。
- 导出时直接从加密文件流式解密到 ZIP 输出流。
- 不在应用缓存目录生成附件明文副本。
- 文件名会过滤路径分隔符、控制字符和 `..`，防止 ZIP 路径穿越。
- 导出前继续要求敏感操作身份验证。
- 用户取消系统保存位置选择时不生成文件。

#### 重要安全边界

该 ZIP 属于“通用导出”，其中资料和附件可被普通解压软件读取，不等同于 KeyScan 加密安全备份。界面必须明确提示：

- 只保存到可信位置；
- 不要上传到不可信服务；
- 迁移或使用完成后及时删除；
- 需要灾难恢复或长期云端保存时，应使用 KeyScan 的加密备份与恢复功能。

#### iOS 实现要求

- 导出 ZIP 的目录结构、`vault.json` 和 `manifest.json` 字段与 Android 保持一致。
- 附件从 iOS 加密存储解密后直接写入 ZIP 流，尽量避免落地临时明文。
- 如平台 ZIP 库必须使用临时文件，必须放在受保护的临时目录，并在成功、失败或取消后立即删除。
- 导出前使用 Face ID、Touch ID 或应用 PIN 完成敏感操作验证。
- iOS 分享面板仅在 ZIP 完整写入并关闭后出现。
- 导出失败时不得留下不完整文件；如系统文件提供器已创建目标，应尽可能删除或标记失败。
- 本轮 Android 的保险箱导入仍只接受原文字 JSON；ZIP 附件重新导入应作为后续独立功能设计，不应误导用户认为通用 ZIP 已支持一键恢复。

#### 验收标准

1. 无附件时也能导出 ZIP，并包含 `vault.json` 和空的 `manifest.json`。
2. 有一个或多个附件时，ZIP 包含全部附件。
3. 同名附件不会互相覆盖。
4. `manifest.json` 的路径、大小和哈希与实际文件对应。
5. 图片、PDF、文本及其他二进制文件导出后均可正常打开。
6. 大附件使用流式处理，不因一次性读入内存导致崩溃。
7. 导出不会改变或删除保险箱中的原始资料和附件。
8. 导出过程不会在缓存目录留下明文附件。

### Android 对应文件

- `app/src/main/java/com/secureqr/scanner/ui/security/SecurityCenterFragment.java`
- `app/src/main/java/com/secureqr/scanner/ui/security/SecurityRecoveryFragment.java`
- `app/src/main/java/com/secureqr/scanner/ui/password/PasswordForgeFragment.java`
- `app/src/main/java/com/secureqr/scanner/ui/password/PasswordEntryAdapter.java`
- `app/src/main/java/com/secureqr/scanner/ui/vault/VaultFragment.java`
- `app/src/main/java/com/secureqr/scanner/data/repository/VaultRepository.java`
- `app/src/main/res/values/strings_recovery_protection.xml`
- `app/src/main/res/values-en/strings_recovery_protection.xml`
# 2026-07-29 — 帮助中心说明书重整

# 2026-07-29 — 备份与恢复入口视觉对齐

## Android 调整

- “备份与恢复”顶部三项使用渐变功能卡：WebDAV 同步配置（蓝）、在线备份与恢复（青绿）、本地备份与恢复（紫）。
- 图标分别使用云端加号、循环同步、文件下载，置于对应颜色的圆形底座中。
- 卡片的原有点击、折叠展开、测试连接、同步、恢复和本地备份逻辑不变。

## iOS 对齐要求

- 使用相同的三张渐变功能卡与图标语义；不要以通用齿轮或单一云朵替代。
- 卡片点击进入/展开现有功能；保留状态摘要和右侧前进指示。

## Android 调整

- 原帮助弹窗改为独立的帮助说明书页面，顶部提供“简要版 / 完整版”切换。
- 两个版本均提供有序目录导航；完整版包含：快速开始、主要功能、安全中心、备份与恢复、公共 WebDAV、家庭 NAS、故障排查、意见反馈。
- 密码账本帮助明确：默认分组和用户自定义文件夹初始均折叠；“测试”只是示例中的用户自定义文件夹名称。
- 安全分享帮助明确：仅保留列表项目长按分享，不设置右上角分享按钮。
- 安全保险箱帮助明确：导出时支持包含附件。
- WebDAV 地区口径：
  - InfiniCLOUD：日本及亚洲地区可优先考虑，免费账户目前支持 WebDAV。
  - Koofr：面向全球多数可注册地区，使用应用专用密码。
  - 坚果云：面向中国大陆可注册用户，使用第三方应用专用密码。
  - Yandex Disk：仅说明可注册 Yandex 账号的地区；自 2026-06-22 起 WebDAV 需要 Yandex 360 付费方案，不再标记为免费 WebDAV。
- NAS 只引导家庭局域网使用：手机与 NAS 连接同一家庭路由器/Wi‑Fi，无需公网暴露；包含群晖、威联通、铁威马的启用入口、局域网地址格式及权限要点。
- 反馈邮箱统一修改为 `userfeedback@zohomail.com`，帮助末尾同时提供应用内“意见反馈”和直接邮件两种方式。
- 帮助页适配应用现有语言：简体中文、繁体中文、英语、日语、韩语、德语、法语、西班牙语、意大利语、荷兰语、巴西葡萄牙语、俄语。
- 完整版新增数据生命周期章节：
  - 导出数据与离线备份：区分“可恢复的加密备份”和模块级导出；说明密码账本 Excel、TOTP URI/加密 JSON、安全保险箱附件及不包含的安全凭证。
  - 导入数据与恢复规则：导入前检查、预览、合并与较新记录覆盖规则、TOTP 导入、换机恢复及密钥丢失的不可逆限制。
  - 回收站、完整性验证与备份习惯：回收站保留期（7/30/90 天或永久）、恢复/永久删除差异、验证的范围与局限、重要操作后的备份节奏。

## iOS 对齐要求

- iOS 帮助入口同样进入独立说明书页，不使用只能连续滚动的单个长弹窗。
- 顶部保留简要版/完整版切换，目录点击后滚动到对应章节，并提供“返回目录”。
- 内容结构、WebDAV 免费状态说明、家庭 NAS 的局域网安全提示及反馈邮箱与 Android 保持一致。
- iOS 也必须对齐导出、导入、回收站、完整性验证、本地/网络备份和换机恢复章节；涉及不可逆操作时使用明确二次确认与风险提示。

## 悬浮窗帮助章节（Android / iOS 对齐）

- 完整版帮助新增“悬浮窗全部功能说明”，按悬浮菜单固定顺序说明全部 12 个入口：智能扫描、分享中心、随机密码、安全保险箱、密码账本、TOTP 认证器、设置、安全中心、备份与恢复、导出数据、回收站、历史记录。
- 每项至少说明用途、主要操作边界和敏感数据注意事项；快捷入口只能缩短导航，不能跳过应用解锁、生物验证或数据保护密钥验证。
- 不得在帮助文案或 UI 中预设任何用户文件夹名称。用户建立的文件夹由用户自行删除、重命名或管理。
- 外部服务政策可能变化，文案中保留“以服务商当前页面为准”的提示。
